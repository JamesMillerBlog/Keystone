extends ../layouts/sketchfab

block intro
	.container
		// h1#h1header Audiences

block content
	// p.categories= audiences.categories
	#loadingScreen
	#sketchContainer
		if locals.data
			each audience in locals.data.active
				p.url= audience.url
				p.link= '/audiences/' + audience.slug
				p.name= audience.title
				// p.categories= audience.categories

block js
	script(src='/audiences/js/bundle.min.js')
	
								
